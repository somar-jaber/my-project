# Todo
- add the npm commands like run

- upload and do the github ReadME file
    - mention the technologies that you used : Node.js , prisma, postgerSQL

- activate logs

- ** STUDY the event loop in JS **


# To setup the prisam 
- npx prisma init
- npm install prisma@6 --save-dev
- npm install @prisma/client@6 
- npx prisma generate 


# To migrate 
- npx prisma migrate dev --name the_name_of_the_migration 
- npx prisma generate 


# To Write about 
- we used JWT , we used the "http only" method to forbid the html in the client side to read the token 
- The API has a tolerance system so it will not fall when an error accurse


# Advices 
## 1
to support modules add the field "type" to package.json file:
{
    ...
    "type" : "module"
    ...
}
and write "commonjs" if you want to get it back to its default value.

## 2
sameSite: "strict", // this will stop the browser from sending those cookie on cross-side requests, to protect from CSRF attacks
                    // что обеспечит защиту от атак CSRF




